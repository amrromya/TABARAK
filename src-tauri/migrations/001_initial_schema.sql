-- Migration 001: Initial schema
-- This migration is a marker for the versioning system.
-- The actual schema is created by the existing migrate() function in db.rs.
-- Future migrations should add ALTER TABLE, CREATE TABLE, CREATE INDEX, etc.
SELECT 1;
